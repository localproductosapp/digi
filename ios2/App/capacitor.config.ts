import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.localdescuentos.Digitalizap',
  appName: 'Digitalizap',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
